{% for item in item.children %}
    {% if item.uri is not empty %}
        <a href="{{ item.uri }}" class="{% if item.current %}dashboard-nav-link-active{% else %}dashboard-nav-link{% endif %}">{{ item.label }}</a>
    {% else %}
        <span>{{ item.label }}</span>
    {% endif %}
{% endfor %}
